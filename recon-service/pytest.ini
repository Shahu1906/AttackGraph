[pytest]
# AttackGraphX Recon Service — pytest configuration

testpaths = tests
asyncio_mode = auto

# Show verbose output with short tracebacks
addopts = -v --tb=short

# Markers
markers =
    unit: Unit tests (no external dependencies)
    integration: Integration tests (requires Docker and/or nmap)
