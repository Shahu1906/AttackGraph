# ─────────────────────────────────────────────────────────────────────────────
# AttackGraphX Recon Service — Python Dependencies
# ─────────────────────────────────────────────────────────────────────────────

# Web framework
fastapi==0.115.0
uvicorn[standard]==0.30.6

# Data validation & settings
pydantic==2.9.2
pydantic-settings==2.5.2

# Redis async client
redis[asyncio]==5.1.0

# XML parsing (secure — prevents XXE)
defusedxml==0.7.1

# Scheduler for periodic scans
apscheduler==3.10.4

# HTTP client (used in health checks only)
httpx==0.27.2

# ─── Testing ──────────────────────────────────────────────────────────────────
pytest==8.3.3
pytest-asyncio==0.24.0
testcontainers==4.8.1
